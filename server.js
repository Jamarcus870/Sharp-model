const https = require('https'); const http = require('http'); let cachedProps = []; let lastFetch = null; function setCORSHeaders(res) { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS, HEAD'); res.setHeader('Access-Control-Allow-Headers', 'Origin, X-Requested-With, Content-Type, Accept, Authorization'); res.setHeader('Access-Control-Allow-Credentials', 'false'); res.setHeader('Access-Control-Max-Age', '86400'); } function fetchProps() { return new Promise((resolve, reject) => { const options = { hostname: 'api.prizepicks.com', path: '/projections?per_page=250&single_stat=true&league_id=2', method: 'GET', headers: { 'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.0 Mobile/15E148 Safari/604.1', 'Accept': 'application/json, text/plain, */*', 'Accept-Language': 'en-US,en;q=0.9', 'Referer': 'https://app.prizepicks.com/', 'Origin': 'https://app.prizepicks.com', 'Connection': 'keep-alive', } }; const req = https.request(options, (res) => { let chunks = []; res.on('data', chunk => chunks.push(chunk)); res.on('end', () => { try { const raw = Buffer.concat(chunks).toString(); console.log('PrizePicks status:', res.statusCode, 'length:', raw.length); if (res.statusCode !== 200) { reject(new Error('Status ' + res.statusCode)); return; } const json = JSON.parse(raw); const players = {}; const leagues = {}; if (json.included) { json.included.forEach(item => { if (item.type === 'new_player') { players[item.id] = { name: item.attributes?.name || 'Unknown', team: item.attributes?.team || '' }; } if (item.type === 'league') { leagues[item.id] = item.attributes?.name || ''; } }); } const props = (json.data || []).map(proj => { const playerId = proj.relationships?.new_player?.data?.id; const leagueId = proj.relationships?.league?.data?.id; const player = players[playerId] || {}; const leagueName = leagues[leagueId] || ''; const ln = leagueName.toUpperCase(); let sport = 'OTHER'; if (ln.includes('NBA')) sport = 'NBA'; else if (ln.includes('MLB')) sport = 'MLB'; else if (ln.includes('WNBA')) sport = 'WNBA'; else if (ln.includes('NFL')) sport = 'NFL'; else if (ln.includes('NHL')) sport = 'NHL'; const attrs = proj.attributes || {}; return { id: proj.id, player: player.name || 'Unknown', team: player.team || '', sport, stat: attrs.stat_type || attrs.stat_display_name || '—', line: parseFloat(attrs.line_score || attrs.stat_score || 0), flash: attrs.flash_sale_line_score || null, isPromo: attrs.is_promo || false, gameTime: attrs.start_time || null, opponent: attrs.opponent_name || '' }; }).filter(p => p.line > 0); cachedProps = props; lastFetch = new Date().toISOString(); console.log('Fetched', props.length, 'props'); resolve(props); } catch (e) { reject(e); } }); }); req.on('error', reject); req.setTimeout(20000, () => { req.destroy(); reject(new Error('Timeout')); }); req.end(); }); } fetchProps().catch(err => console.error('Initial fetch failed:', err.message)); setInterval(() => { fetchProps().catch(err => console.error('Refresh failed:', err.message)); }, 25 * 60 * 1000); const server = http.createServer((req, res) => { setCORSHeaders(res); res.setHeader('Content-Type', 'application/json'); if (req.method === 'OPTIONS') { res.writeHead(204); res.end(); return; } if (req.method === 'HEAD') { res.writeHead(200); res.end(); return; } const url = req.url.split('?')[0]; if (url === '/props' || url === '/') { res.writeHead(200); res.end(JSON.stringify({ props: cachedProps, count: cachedProps.length, lastFetch, status: 'ok' })); return; } if (url === '/health') { res.writeHead(200); res.end(JSON.stringify({ status: 'ok', lastFetch, count: cachedProps.length, uptime: process.uptime() })); return; } res.writeHead(404); res.end(JSON.stringify({ error: 'Not found' })); }); const PORT = process.env.PORT || 3000; server.listen(PORT, '0.0.0.0', () => { console.log('Sharp server running on port', PORT); });
